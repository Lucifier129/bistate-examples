"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var react_1 = require("react");
var createStore_1 = __importDefault(require("../createStore"));
var createBistate_1 = require("../createBistate");
var util_1 = require("../util");
function useBistate(initialState) {
    var store = react_1.useMemo(function () {
        if (util_1.isFunction(initialState)) {
            initialState = initialState();
        }
        return createStore_1.default(initialState);
    }, []);
    var _a = __read(react_1.useState(store.getState()), 2), state = _a[0], setState = _a[1];
    var updateState = react_1.useCallback(function (nextState) {
        var currentState = store.getState();
        if (util_1.isFunction(nextState)) {
            createBistate_1.mutate(function () { return nextState(currentState); });
        }
        else {
            createBistate_1.mutate(function () { return util_1.merge(currentState, nextState); });
        }
    }, [store]);
    react_1.useLayoutEffect(function () { return store.subscribe(setState); }, [store]);
    return [state, updateState];
}
exports.default = useBistate;
//# sourceMappingURL=useBistate.js.map