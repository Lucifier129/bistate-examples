"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var react_1 = require("react");
var useBistate_1 = __importDefault(require("./useBistate"));
var util_1 = require("../util");
function useBireducer(reducer, initialState) {
    var _a = __read(useBistate_1.default(initialState), 2), state = _a[0], updateState = _a[1];
    var reducerRef = react_1.useRef(reducer);
    var dispatch = react_1.useCallback(function (action) {
        updateState(function (currentState) {
            reducerRef.current(currentState, action);
        });
    }, []);
    react_1.useLayoutEffect(function () {
        if (!util_1.isFunction(reducer)) {
            throw new Error('reducer must be a function');
        }
        reducerRef.current = reducer;
    }, [reducer]);
    return [state, dispatch];
}
exports.default = useBireducer;
//# sourceMappingURL=useBireducer.js.map